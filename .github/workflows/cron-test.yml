name: ТЕСТ расписания

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: |
          date -u '+Сработало по расписанию: %Y-%m-%d %H:%M UTC'
